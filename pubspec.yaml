name: snip
description: A CLI tool that formats Dart code snippets embedded in Markdown files and doc comments.
version: 0.1.0

executables:
  snip:

environment:
  sdk: ^3.11.1

dependencies:
  args: ^2.6.0
  dart_style: ^3.1.0
  glob: ^2.1.3
  path: ^1.9.0
  pub_semver: ^2.1.5

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
